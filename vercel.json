{
  "rewrites": [
    { "source": "/webhook/:tipo", "destination": "/api/webhook/:tipo" },
    { "source": "/metrics", "destination": "/api/metrics" }
  ],
  "functions": {
    "api/ghl-sync.ts": { "maxDuration": 55 },
    "api/ghl-debug.ts": { "maxDuration": 45 }
  }
}
